# Gradle
.gradle/
build/

# Kotlin
.kotlin/

# Kotlin/JS — keep kotlin-js-store/yarn.lock committed for reproducible CI builds

# IDE
.idea/
*.iml
*.ipr
*.iws
.fleet/
.vscode/

# OS
.DS_Store

# Local Gradle config / secrets
local.properties

# Local dev server script — carries a real OpenRouter key once filled in
tools/dev-server.sh

# The server's default sqlite file, written wherever it runs with no STRAMUS_DB set — `:server:test` and
# `tools/dev-server.sh` both do this from the repo root.
server/stramus.db*

# The generator in tools/icon-data leaves these behind when it is imported rather than run.
__pycache__/
